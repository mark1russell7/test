/**
 * Integration Tests
 *
 * Tests for Docker-based services using the ecosystem's procedure infrastructure.
 * Run with: pnpm test:integration
 *
 * Structure:
 * - docker/mongo.integration.test.ts - MongoDB container lifecycle tests
 * - docker/sqlite.integration.test.ts - SQLite container lifecycle tests
 * - client/full-stack.integration.test.ts - Client procedure chain tests
 *
 * Note: Test files are run by vitest, not exported as modules.
 */
export {};
//# sourceMappingURL=index.d.ts.map